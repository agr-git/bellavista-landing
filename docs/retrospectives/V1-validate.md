# V1 validation — Bellavista Landing

**Date:** 2026-07-13  
**Branch:** `test/v1-production-validation`  
**Issue:** #3 — V1 production validation  
**Base URL:** https://www.bellavista-coffee.com.co

## Executive result

V1 validation is **not green yet** because production is not reconciled with `main`.

The public landing routes are reachable, but auth/members and OAuth legal routes currently return 404 in production. This matches the known scope of issue #1 and should be re-run after the production/auth reconciliation PR is approved, merged, and deployed.

## Resend / Notion scope

The Resend + Notion production lead-funnel E2E is **DEFERRED**, not failed, because issue #2 is intentionally parked while the PM decision is still in discussion.

This report does not claim final lead-funnel acceptance.

## Local repository checks

Executed from this branch before publishing the evidence:

```bash
npm ci
npm run lint
npm run validate:content
npm run build
npm run validate:v1
```

Results:

- `npm ci`: completed.
- `npm run lint`: passed; no ESLint warnings or errors.
- `npm run validate:content`: passed; 5 entries validated, 5 public.
- `npm run build`: passed; Next.js production build completed.
- `npm run validate:v1`: completed and correctly failed production auth/member gates.

## Production route evidence

Generated by `npm run validate:v1` at `2026-07-13T14:32:35.933Z`.

| Status | Gate | Path | Expected | Actual |
|---|---|---|---|---|
| PASS | public-home | `/` | 200 | 200 |
| PASS | seo | `/robots.txt` | 200 | 200 |
| PASS | seo | `/sitemap.xml` | 200 | 200 |
| FAIL | auth-members | `/login` | 200 | 404 |
| FAIL | auth-members | `/members` | 200/302/307/308 | 404 |
| FAIL | oauth-legal | `/privacy` | 200 | 404 |
| FAIL | oauth-legal | `/terms` | 200 | 404 |
| FAIL | auth-members | `/api/auth/providers` | 200 | 404 |
| FAIL | auth-members | `/api/auth/session` | 200 | 404 |

## Interpretation

- Production is serving an older deployed build than repository `main`.
- The V1 route validator is now versioned in the repo as `scripts/validate-v1.mjs` and can be re-run after deploy.
- Issue #1 remains the blocking gate for final V1 acceptance.
- Issue #2 remains deferred until the PM decision is resolved.

## Next validation run

After issue #1 is deployed:

```bash
npm run validate:v1
```

Expected auth/member results after reconciliation:

- `/login` returns 200.
- `/members` returns 200 or redirects to login/auth as expected.
- `/privacy` and `/terms` return 200.
- `/api/auth/providers` returns 200 with Google configured.
- `/api/auth/session` returns 200.

Only after those pass should this report be updated from **not green yet** to final V1 acceptance.
