/**
 * Stay (Chapter 05 — Farmhouse). B3 stub; filled in B7.
 */

export default function Stay() {
  return (
    <section id="stay" className="py-20 px-10 border-t border-line">
      <p className="font-mono text-meta uppercase text-ink-3">
        [B3 stub] Section 06 · Stay — bento grid + price card lands in B7
      </p>
    </section>
  );
}
