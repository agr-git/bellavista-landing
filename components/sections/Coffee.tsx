/**
 * Coffee (What we grow). B3 stub; filled in B7.
 */

export default function Coffee() {
  return (
    <section id="coffee" className="py-20 px-10 border-t border-line">
      <p className="font-mono text-meta uppercase text-ink-3">
        [B3 stub] Section 05 · Coffee — B2B/Direct split + pull quote lands in B7
      </p>
    </section>
  );
}
