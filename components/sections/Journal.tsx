/**
 * Journal (From the field). B3 stub; filled in B8 (MDX reader).
 */

export default function Journal() {
  return (
    <section id="journal" className="py-20 px-10 border-t border-line">
      <p className="font-mono text-meta uppercase text-ink-3">
        [B3 stub] Section 07 · Journal — MDX reader + featured post lands in B8
      </p>
    </section>
  );
}
