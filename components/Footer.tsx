/**
 * Footer / Contact slab. B3 stub; filled in B7.
 */

export default function Footer() {
  return (
    <footer id="contact" className="bg-surface border-t border-line py-10 px-10">
      <p className="font-mono text-meta uppercase text-ink-3">
        [B3 stub] Contact slab — address + subscribe + admin lands in B7
      </p>
    </footer>
  );
}
